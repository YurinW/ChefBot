"""
Database domain package.

Contains SQLAlchemy ORM models and related helpers. Import `chefbot.infrastructure.persistence.db.models`
for the concrete table definitions instead of the older `chefbot.domain.models.*`
modules.
"""

