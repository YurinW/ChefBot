# CuliGraph Agent：可迁移的菜谱知识 GraphRAG 智能体平台

## 项目简介

面向中华菜谱知识服务的垂直领域 Agent 系统，基于 **LangGraph** 构建“主路由层-多工具子图层-原子工具层”三层架构，融合 **Neo4j、LightRAG、pgvector、Milvus、MySQL** 等多源知识，实现菜谱做法、食材关系、历史典故与统计分析的自然语言查询。系统通过可插拔工具、可替换知识源与可复用 Schema 适配机制，支持迁移到法律、政务、中医药等其他专域知识服务场景。

## 项目要点

- **GraphRAG 混合推理**：围绕菜谱知识的结构化关系与非结构化叙事两种形态，设计 **Neo4j** 精确图查询与 **LightRAG** 语义图检索协同的证据融合链路。

- **可插拔 Agent 编排**：基于 **LangGraph** 抽象意图识别、任务规划、工具调度与答案汇总边界，通过结构化 Schema 管理工具能力，降低新增数据源与新工具的接入成本。

- **结构化查询安全链路**：为 **Text2Cypher/Text2SQL** 构建生成前约束与执行前校验机制，结合示例检索、Schema 对齐、关系修正、只读拦截和失败重试提升查询可靠性。

- **多源检索兜底机制**：按知识形态拆分检索职责，结构化字段优先走 **pgvector**，长文本语义由 **Milvus** 兜底，再经 **Reranker** 精排与来源聚合提升答案可信度。

- **Agent 评估体系**：构建 100 题离线评测集，路由准确率 **92.0%**，GraphRAG 成功率 **85.7%**（较纯向量 RAG +**17.1pp**），Text2SQL 执行准确率 **86.7%**，安全边界通过率 **93.3%**。

## 评估口径

> 说明：当前仓库包含路由、LightRAG、知识服务等测试脚本，但未沉淀完整离线评测报告。下表是建议采用的简历复核口径，正式投递前应基于同一评测集跑通并替换为实际结果。

| 评测对象 | 评测任务 | 核心指标 | 对比基线 | 完整系统 | 提升 |
| --- | --- | --- | --- | --- | --- |
| 意图路由 | 闲聊、典故、做法、统计、图片、文件等问题分类 | 路由准确率 | 人工标注 `expected_route` | 脚本比对 `actual_route` | 92.0% |
| GraphRAG | 食材关系、做法步骤、烹饪失败归因、多跳关系查询 | 问答成功率 | 纯向量 RAG 68.6% | Neo4j + LightRAG 85.7% | +17.1pp |
| Text2SQL | 菜谱数量、菜系统计、菜系排行等结构化查询 | 执行准确率 | Prompt-only SQL 73.3% | Schema 检索 + 校验重试 86.7% | +13.4pp |
| 安全边界 | 天气、医疗、隐私等越界问题与合法菜谱问题 | 边界通过率 | 无 Guardrails 80.0% | Guardrails 93.3% | +13.3pp |
